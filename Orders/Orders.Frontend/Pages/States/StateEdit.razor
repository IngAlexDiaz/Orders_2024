@page "/states/edit/{StateId:int}"

<h3>Editar Estado / Provincia</h3>

@if (state is null)
{
    <Loading />
}
else
{
    <FormWithName @ref="stateForm" Label="Estado / Provincia" Model="state" OnValidSubmit="SaveAsync" ReturnAction="Return" />
}